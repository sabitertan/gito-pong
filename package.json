{
  "name": "gito",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "gh-pages": "^6.3.0",
    "playwright": "^1.53.0",
    "vite": "^6.3.5"
  }
}
